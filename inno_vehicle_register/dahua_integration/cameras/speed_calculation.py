from collections import defaultdict
import os

import random


def speed_calculation():
    # Speed calculation is not required
    # Each vehicle should be went under at least 1 camera in order to calculate average speed
    speed_list = [87, 60, 54, 123, 65, 132, 80, 78, 83, 90, 92, 79, 74]
    random_distance = random.choice(speed_list)


    return random_distance









